<div id="nav-links" class="nav-links collapsed font-overpass-mono capitalize">
  <a class="fade-effect" href="/">Home</a>
  <a class="fade-effect" href="#fragrance">The Fragrance</a>
  <a class="fade-effect" href="#ingredients">Ingredients</a>
  <a class="fade-effect" href="#craft">Our Craft</a>
  <a class="fade-effect" href="#ritual">The Ritual</a>
  <slot />
</div>
<style>
  /* nav styles */
  .nav-links {
    position: absolute;
    width: 300px;
    height: auto;
    right: -100%;
    padding: 10px;
    transition: opacity 0.2s ease-in-out;
    opacity: 0;
    z-index: 10;
    pointer-events: none;
  }
  .nav-links.collapsed {
    right: 0;
    opacity: 0;
    top: 25px;
    border-radius: 14px;
    box-shadow: 0 12px 30px -8px rgba(46, 42, 34, 0.28);
  }

  .nav-links.open {
    border-radius: 14px;
    right: 0;
    top: 25px;
    opacity: 1;
    box-shadow: 0 12px 30px -8px rgba(46, 42, 34, 0.28);
    pointer-events: auto;
  }

  .nav-links a {
    display: block;
    line-height: 50px;
    padding-left: 30px;
    box-sizing: border-box;
    font-size: 18px;
    text-decoration: none;
    height: auto;
  }

  html.dark .nav-links {
    background: #2a2f25;
    border: 1px solid rgba(138, 154, 123, 0.18);
  }

  html:not(.dark) .nav-links {
    background: #f3efe6;
    border: 1px solid rgba(46, 42, 34, 0.1);
  }
  .fade-effect {
    transition: opacity 0.5s ease-in-out;
    opacity: 1;
  }

  .fade-effect:hover {
    opacity: 0.5;
  }
  .nav-search {
    display: flex;
    align-items: center;
    padding: 10px;
    width: 100%; /* make the search box take up the whole width */
  }
</style>
